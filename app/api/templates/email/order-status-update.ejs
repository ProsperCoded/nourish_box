<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Status Update - <%= companyName %></title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #374151;
            max-width: 600px;
            margin: 0 auto;
            padding: 0;
            background-color: #f9fafb;
        }
        .email-container {
            background: #ffffff;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            overflow: hidden;
            margin: 20px;
        }
        .header {
            background: #22c55e;
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0 0 5px 0;
            font-size: 24px;
            font-weight: 600;
        }
        .header p {
            margin: 0;
            font-size: 14px;
            opacity: 0.9;
        }
        .content {
            padding: 30px;
        }
        .status-alert {
            padding: 20px;
            border-radius: 6px;
            margin: 20px 0;
            border-left: 4px solid;
        }
        .order-summary {
            background: #f9fafb;
            padding: 20px;
            border-radius: 6px;
            margin: 20px 0;
            border: 1px solid #e5e7eb;
        }
        .order-summary h3 {
            color: #1f2937;
            margin-top: 0;
            font-size: 16px;
            font-weight: 600;
        }
        .order-id {
            font-size: 16px;
            font-weight: bold;
            color: #22c55e;
        }
        .recipes-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            border-radius: 6px;
            overflow: hidden;
            border: 1px solid #e5e7eb;
        }
        .recipes-table th {
            background: #22c55e;
            color: white;
            padding: 12px;
            text-align: left;
            font-weight: 600;
            font-size: 14px;
        }
        .recipes-table td {
            padding: 12px;
            border-bottom: 1px solid #e5e7eb;
            background: #ffffff;
            font-size: 14px;
        }
        .total-row {
            background: #f9fafb;
            font-weight: bold;
        }
        .delivery-info {
            background: #eff6ff;
            padding: 20px;
            border-radius: 6px;
            margin: 20px 0;
            border-left: 4px solid #3b82f6;
        }
        .delivery-info h3 {
            color: #1d4ed8;
            margin-top: 0;
            font-size: 16px;
            font-weight: 600;
        }
        .track-button {
            background: #22c55e;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 600;
            display: inline-block;
            margin: 20px 0;
            font-size: 14px;
        }
        .track-button:hover {
            background: #16a34a;
        }
        .footer {
            background: #f9fafb;
            padding: 25px;
            text-align: center;
            border-top: 1px solid #e5e7eb;
        }
        .footer p {
            margin: 6px 0;
            color: #6b7280;
            font-size: 13px;
        }
        .support-info {
            background: #eff6ff;
            padding: 12px;
            border-radius: 4px;
            margin-top: 15px;
            border-left: 3px solid #3b82f6;
        }
        .support-info strong {
            color: #1d4ed8;
        }
        @media (max-width: 600px) {
            .email-container {
                margin: 10px;
                border-radius: 8px;
            }
            .header {
                padding: 30px 20px;
            }
            .content {
                padding: 30px 20px;
            }
            .footer {
                padding: 25px 20px;
            }
            .track-button {
                display: block;
                text-align: center;
            }
            .recipes-table {
                font-size: 14px;
            }
            .recipes-table th, .recipes-table td {
                padding: 8px;
            }
        }
    </style>
</head>
<body>
    <%
        // Get status-specific styling and messaging
        function getStatusInfo(status) {
            const statusLower = status.toLowerCase();
            switch (statusLower) {
                case 'pending':
                    return {
                        emoji: '',
                        color: '#f59e0b',
                        bgColor: '#fef3c7',
                        title: 'Order Confirmed - Preparing Your Recipes',
                        message: 'Your order is being processed and will be prepared shortly.',
                        actionText: 'Your recipes are being prepared.'
                    };
                case 'packed':
                    return {
                        emoji: '',
                        color: '#3b82f6',
                        bgColor: '#dbeafe',
                        title: 'Order Packed - Ready for Delivery',
                        message: 'Your recipes have been prepared and packed for delivery.',
                        actionText: 'Your order is ready and will be dispatched soon.'
                    };
                case 'in_transit':
                    return {
                        emoji: '',
                        color: '#8b5cf6',
                        bgColor: '#ede9fe',
                        title: 'Order In Transit - On the Way to You',
                        message: 'Your order is currently being delivered to your location.',
                        actionText: `Your delivery person will arrive soon. Show them this Order ID: <strong style="background: #fef3c7; padding: 4px 8px; border-radius: 4px; color: #92400e;">#${orderId.slice(-8).toUpperCase()}</strong>`
                    };
                case 'delivered':
                    return {
                        emoji: '',
                        color: '#10b981',
                        bgColor: '#d1fae5',
                        title: 'Order Delivered - Enjoy Your Meal!',
                        message: 'Your order has been successfully delivered.',
                        actionText: 'We hope you enjoy cooking and eating your delicious recipes!'
                    };
                case 'failed':
                    return {
                        emoji: '',
                        color: '#ef4444',
                        bgColor: '#fee2e2',
                        title: 'Delivery Issue - Please Contact Support',
                        message: 'There was an issue with your delivery.',
                        actionText: 'Please contact our support team for assistance.'
                    };
                default:
                    return {
                        emoji: '',
                        color: '#6b7280',
                        bgColor: '#f3f4f6',
                        title: 'Order Status Updated',
                        message: `Your order status has been updated to: ${status}`,
                        actionText: 'Thank you for your patience.'
                    };
            }
        }

        const statusInfo = getStatusInfo(currentStatus);
    %>

    <div class="email-container">
        <div class="header">
            <h1>Order Status Update</h1>
            <p><%= companyName %> - Order Tracking</p>
        </div>

        <div class="content">
            <div class="status-alert" style="background: <%= statusInfo.bgColor %>; border-left-color: <%= statusInfo.color %>;">
                <h2 style="color: <%= statusInfo.color %>; margin-top: 0; font-size: 18px; font-weight: 600;">
                    <%= statusInfo.title %>
                </h2>
                <p style="font-size: 14px; margin: 12px 0;"><strong><%= statusInfo.message %></strong></p>
                <p style="font-size: 14px; color: #6b7280;"><%- statusInfo.actionText %></p>
                <% if (currentStatus.toLowerCase() === 'delivered') { %>
                <div style="background: #f0fdf4; padding: 12px; border-radius: 4px; margin-top: 15px; border: 1px solid #bbf7d0;">
                    <p style="margin: 0; color: #166534; font-weight: 600; font-size: 13px;">Enjoy your meal and don't forget to rate your experience!</p>
                </div>
                <% } %>
            </div>

            <div class="order-summary">
                <h3>Order Summary</h3>
                <p class="order-id">Order ID: #<%= orderId.slice(-8).toUpperCase() %></p>
                <p><strong>Customer:</strong> <%= customerName %></p>
                <p><strong>Status Updated:</strong> <%= new Date(updatedAt).toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                }) %></p>
                <% if (currentStatus.toLowerCase() === 'in_transit') { %>
                <p style="background: #eff6ff; padding: 8px; border-radius: 4px; margin-top: 12px; border: 1px solid #bfdbfe; font-size: 13px;">
                    <strong>Tip:</strong> Have your Order ID ready: <strong>#<%= orderId.slice(-8).toUpperCase() %></strong>
                </p>
                <% } %>
            </div>

            <table class="recipes-table">
                <thead>
                    <tr>
                        <th>Recipe</th>
                        <th style="text-align: right;">Price</th>
                    </tr>
                </thead>
                <tbody>
                    <% recipes.forEach(recipe => { %>
                    <tr>
                        <td>
                            <strong><%= recipe.name %></strong>
                            <% if (recipe.description) { %>
                            <br><small style="color: #6b7280;"><%= recipe.description %></small>
                            <% } %>
                        </td>
                        <td style="text-align: right;">
                            <strong>
                                <%= new Intl.NumberFormat('en-NG', {
                                    style: 'currency',
                                    currency: 'NGN'
                                }).format(recipe.price) %>
                            </strong>
                        </td>
                    </tr>
                    <% }) %>
                    <tr class="total-row">
                        <td><strong>Total Amount</strong></td>
                        <td style="text-align: right;">
                            <strong style="font-size: 18px;">
                                <%= new Intl.NumberFormat('en-NG', {
                                    style: 'currency',
                                    currency: 'NGN'
                                }).format(orderAmount) %>
                            </strong>
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="delivery-info">
                <h3>Delivery Information</h3>
                <p><strong>Delivery Address:</strong></p>
                <p style="margin-left: 20px; color: #4a5568;">
                    <%= deliveryAddress %><br>
                    <%= deliveryCity %>, <%= deliveryState %>
                </p>
                <% if (currentStatus.toLowerCase() === 'in_transit') { %>
                <p style="background: #eff6ff; padding: 8px; border-radius: 4px; margin-top: 12px; border: 1px solid #bfdbfe; font-size: 13px;">
                    <strong>Delivery in progress!</strong> Please stay available at this address.
                </p>
                <% } %>
            </div>

            <div style="text-align: center; margin: 25px 0;">
                <a href="<%= websiteUrl %>/profile?tab=track" class="track-button">View All Orders</a>
            </div>
        </div>

        <div class="footer">
            <p><strong>Thank you for choosing <%= companyName %>!</strong></p>
            <p>We appreciate your patience as we prepare your delicious recipes!</p>

            <div class="support-info">
                <p><strong>Need Help?</strong></p>
                <p style="font-size: 13px; margin: 4px 0;">Our support team is here to assist you:</p>
                <p style="font-size: 13px; margin: 4px 0;">Email: <strong><%= companyEmail || 'hello@nourishboxng.com.com' %></strong></p>
                <p style="font-size: 13px; margin: 4px 0;">We typically respond within 24 hours</p>
                <p style="margin-top: 8px; font-size: 12px; color: #6b7280;">
                    Order questions? Include your Order ID: #<%= orderId.slice(-8).toUpperCase() %>
                </p>
            </div>
        </div>
    </div>
</body>
</html>
