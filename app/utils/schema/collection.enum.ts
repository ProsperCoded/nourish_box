export enum COLLECTION {
  recipes = "recipes",
  users = "users",
  comments = "comments",
  favorites = "favorites",
  carts = "carts",
  // payments = "payments",
  // transactions = "transactions",
}
